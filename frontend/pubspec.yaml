name: ai_inventory_frontend
description: Enterprise Inventory Management System Frontend

publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^1.1.0
  fl_chart: ^0.65.0
  google_fonts: ^6.1.0
  dio: ^5.4.0
  file_picker: ^8.0.0
  web_socket_channel: ^2.4.0
  timelines_plus: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
